multiple types are missing commans after the closing bracket
comparing to parent in switch statements
default case in switch statements
there is a slash in some switch comparisons
there is no construct literal called 'Value'
change Constant to Const

-------

change Value to Const
remove commas on top level structs
add default value for switch statements
switch statmenets have quotes around the number
Array -> PrefixedArray
For UUIDS: String(32) -> String(16)
Double check that the void type is handled using 'Pass"
Change numeric endian (Int32sn is incorrect! use Int32sb etc.)
Change String(16) to PaddedString(16, "utf8")
OPTIONAL:
Add metadata type
